# Pi SSH credentials — copy to .env and fill in your values
PI_USER=pi
PI_HOST=pi.local
PI_PASS=

# Display name shown in the Plink app — add new displays in plink.sh prompt_display_model
# Default: Inky Impression 7.3"
DISPLAY_MODEL=Inky Impression 7.3"
